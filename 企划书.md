# TradeEye Copilot 项目计划书

## 一、 项目概述与核心定位

* **项目名称**：TradeEye Copilot（基于 Skill Agent 与产业链映射的交互式投研 Copilot）
* **核心定位**：专为买方机构（PM/研究员）打造的轻量级协同投研助手。摒弃传统“爬虫+文本摘要”模式，提供**事实级准确的财报数据**、**极速产业链上下文分析**与**飞书原位卡片交互**。
* **架构基石**：采用“确定性数据管道 (Deterministic Pipeline) + 无头认知大脑 (Headless Agent) + 飞书卡片 GUI”的解耦架构。
* **复用项目**：
* `daoyezongzi/TradeEye`：数据抓取、感知管道与飞书推送机制
* `daoyezongzi/PlatoAcademy`：无头 Skill Agent 编排、RAG 检索与推理逻辑



---

## 二、 核心主线：从“严肃数据”到“极简交互”的落地闭环

本项目的核心推进主线极其明确：**以严肃金融场景对“零数字错误”的物理硬约束为底座，通过工程剪枝实现极致吞吐，最终落脚于“零 Prompt 门槛”的飞书卡片原位交互。**

```text
┌─────────────────────────────────────────────────────────┐
│  底座：双轨数据管道（正则/脚本硬校验，确保数字 100% 准确）  │
└────────────────────────────┬────────────────────────────┘
                             │ 纯净数据 + 本地 Context
                             ▼
┌─────────────────────────────────────────────────────────┐
│  中枢：无头 Skill Agent（轻重分离，2~5 分钟极速响应）      │
└────────────────────────────┬────────────────────────────┘
                             │ 结构化输出
                             ▼
┌─────────────────────────────────────────────────────────┐
│  终端：飞书卡片 GUI（一键点击、原位刷新、零 Prompt 交互）   │
└─────────────────────────────────────────────────────────┘

```

---

## 三、 竞赛指标适配与产品区分度（核心创新点）

为了在众多 Agent 类项目中脱颖而出，项目明确区分**基础工程保底**与**核心体验创新**，精准对标 GitCode 竞赛指标：

### 1. 核心创新点（体验与交互区分度）

* **创新点一：零 Prompt 门槛的卡片级 Agent 交互（解法：使用率 > 70%）**
* **设计**：彻底摒弃传统 Chatbot 需要用户手动输入复杂 Prompt 的对话框模式。将后台复杂的 Agent 逻辑（如同业对比、管理层展望追问、产业链推演）封装为飞书卡片底部的预设 Skill 按钮。
* **优势**：买方研究员无需学习提示词工程，一键点击即可触发复杂分析 Skill，极大降低使用门槛，保障高频日活。


* **创新点二：原位刷新（In-place Refresh）与状态保持（解法：降低交互噪音）**
* **设计**：点击卡片按钮后，通过 Webhook 唤醒后台对应的 Skill 后端，直接原位更新当前卡片中的数据与图表，而非在群聊中不断推送新消息。
* **优势**：保持信息流整洁，实现类似 Web 原生 GUI 的交互体验。



### 2. 场景极化适配（面向严肃金融场景的工程硬约束）

* **极化设计一：双轨架构（Dual-Track）与硬校验（解法：准确率 > 95%）**
* **适配逻辑**：在严肃金融场景，大模型处理结构化数字的幻觉是客观规律。本项目**不让 LLM 接触基础算术与表格数字**，所有财务核心指标走正则表达式与结构化解析器存库并进行脚本 Hard-Check，准确率达 100%；LLM 仅在后半程负责非结构化语义推演。


* **极化设计二：轻重分离与 Context 0ms 注入（解法：生成耗时 < 30min）**
* **适配逻辑**：拒绝多 Agent 耗时冗长的动态协商过程。构建本地静态轻量级分类映射树，根据股票代码 **0ms 查表** 自动获取产业链节点，作为 Context 直接注入，将全流程耗时压制在 **2~5 分钟**，同时大幅降低 Token 开销。



---

## 四、 系统架构与分层细节

```text
 ┌───────────────────────────────────────────────────────────────┐
 │                   数据与感知管道层 (TradeEye)                  │
 └───────────────────────────────┬───────────────────────────────┘
                                 │
     ┌───────────────────────────┴───────────────────────────┐
     ▼                                                       ▼
【确定性 Parsing 引擎】                              【静态轻量级分类树】
(财报表格/规则/脚本硬校验存库)                       (产业链节点/敏感因子 0ms 查表)
     │                                                       │
     └───────────────────────────┬───────────────────────────┘
                                 │ 纯净数据 + 产业链 Context
                                 ▼
 ┌───────────────────────────────────────────────────────────────┐
 │               认知与 Skill 执行大脑 (Plato Agent)              │  <-- 【无头后端 API】
 └───────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
 ┌───────────────────────────────────────────────────────────────┐
 │                 交互与展示终端 (飞书 Card GUI)                │  <-- 【原位刷新 UI】
 └───────────────────────────────────────────────────────────────┘

```

### 1. 数据与感知管道层（TradeEye 模块）

* 7×24 小时监控财报与公告 RSS 订阅源，负责信息的实时抓取与分发。

### 2. 确定性处理与知识上下文层（基石保障）

* **结构化 Parsing 引擎**：使用 `Pandas/BeautifulSoup/pdfplumber` 直接解析 HTML/PDF 表格，将营业收入、净利润等核心指标直接写入结构化数据库。
* **脚本数字审查（Hard-Check）**：通过正则与换算逻辑，对数据提取结果进行 100% 匹配校验，未通过者直接丢弃或警告。
* **静态轻量级分类映射树**：本地维护行业与上下游映射树（JSON/SQLite）。根据股票代码 0ms 查表获取产业链位置（上游/中游/下游）与敏感因子，作为 Context 喂给 Agent。

### 3. 认知与 Skill 执行大脑（Plato Agent 内核）

* **无头化重构**：剥离原有的前端 UI，重构为微服务架构的 **Headless Agent**。
* **职责分工**：保留编排引擎、记忆管理与 RAG 检索能力。只接收输入，调用特定 Skill 进行语义总结与逻辑推演，通过 API 输出结构化数据给飞书终端。

### 4. 交互与展示终端（飞书 Card GUI）

* 以飞书交互式卡片完全替代传统 Chatbot 对话框。
* 卡片直观呈现简报摘要与核心数值。
* 卡片底部嵌入 Plato Skill 按钮（如 `[Plato: 一键对比同行业]`、`[Plato: 追问管理层展望]`），点击即唤醒后台对应 Skill 并**原位刷新卡片**。

---

## 五、 关键业务流程

1. **抓取与提取**：TradeEye 捕获某上市公司 RSS 财报更新。 Parsing 引擎与脚本提取核心指标存库，正则脚本通过 Hard-Check 确保数字绝对准确。
2. **上下文注入**：根据股票代码检索静态分类树，获取该公司的产业链位置及上下游关联板块。
3. **Agent 推理**：Plato Agent 结合“纯净财务数据”与“产业链上下文”，调用推理 Skill 生成带有产业链影响预警的简报底稿。
4. **卡片推送与交互**：渲染飞书卡片推送到群聊。研究员点击卡片上的 `[一键对比同行业]` 按钮，触发后台 Plato RAG Skill，卡片实时更新展示同类竞品对比表格。

---

## 六、 实施路线图与阶段规划

### 第一阶段：管道构建与数字硬校验（基石铺设）

* 优化 TradeEye 的 RSS 抓取模块。
* 编写针对标准财报的结构化 Parsing 模块与正则表达式 Hard-Check 校验脚本，完成数据库读写测试。

### 第二阶段：知识映射与 Plato 内核无头化重构（中枢解耦）

* 构建轻量级行业产业链映射树（覆盖重点关注行业与上下游节点）。
* 将 Plato 项目重构为 Headless Agent 服务，暴露标准 API 接口，集成 RAG 检索 Skill。

### 第三阶段：飞书卡片 GUI 整合与原位刷新（交互闭环）

* 设计并开发飞书富文本交互卡片模板。
* 实现飞书卡片按钮 Webhook 与 Plato 后端 Skill 的联动机制，支持卡片的动态原位刷新。

### 第四阶段：指标压测与商业 ROI 验证（竞赛对标）

* **准确率压测**：针对各种复杂财报格式进行边界测试，验证硬校验覆盖率。
* **耗时优化**：优化多线程抓取与 Agent 链条，保证 2~5 分钟内完成输出。
* **使用率验证**：测试卡片一键交互的流畅度与原位刷新稳定性，准备演示 DEMO。